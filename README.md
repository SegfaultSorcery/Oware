## Build Instructions

### Option 1: Using Nix

If you have **Nix** installed, you can run the project directly with:

```sh
nix run github:vebli/Oware
```

### Option 2: Using CMake
```sh
mkdir build
cd build
cmake ..
cmake --build .
./Oware
```
